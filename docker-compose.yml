services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - uploads:/var/www/uploads
      - logs:/var/log/longqing
    restart: unless-stopped

volumes:
  uploads:
  logs:
